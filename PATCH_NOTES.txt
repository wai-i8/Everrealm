Everrealm follow-up UI patch — 2026-09-10
Base: the user's current-thread latest package plus the previously delivered dev/audio/status patch. No files from other threads were used.

Changes in this patch:
1. Mobile exploration rail
   - Enlarges the five illustrated function icons visually to match the music control more closely.
   - Keeps the same tap targets/order and increases spacing to avoid overlap.

2. Skill tree responsive behavior
   - Adds a readable minimum skill-tree canvas size.
   - Large screens stretch naturally.
   - Small screens keep node size/spacing and expose horizontal/vertical scrollbars instead of squeezing the tree.

3. Inventory/equipment redesign
   - Removes the character sprite/paper-doll from the portable inventory loadout.
   - Removes the charm slot from this view.
   - Six slots use a fixed 2x3 arrangement:
       head | weapon
       upper | hands
       lower | feet
   - Empty slots use muted slot-type line icons.
   - Filled slots are icon-only in the overview.
   - Inventory item cards are icon-first with quantity only when >1; equipped state uses a checkmark.
   - Removes the permanent "select an item" detail strip.
   - Selecting an item opens a compact detail popup with no X; tap the dimmed background (or Escape) to dismiss.

4. Cache/version
   - Bumps code/PWA version tokens from 20260910-08 to 20260910-09.

Modified files:
- game.js
- responsive-ui-redesign.css
- index.html
- everrealm-sw.js
- service-worker.js
